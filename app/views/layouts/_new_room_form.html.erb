<%= form_with(model: @room, remote: true, class:'d-flex mt-3') do |f| %>
  <%= f.text_field :name, class: 'form-control', autocomplete: 'off' %>
  <%= f.submit data: {disable_with: false},  class: 'btn btn-success' %>
<% end %>