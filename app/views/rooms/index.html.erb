 <div id="chatroom_container" class="row bg-custom text-light pt-4 mt-5">
  <div id="side_panel" class="col-md-4 col-sm-4">

    <button type="button" class="btn btn-warning mb-3"><strong>HotChatter</strong> <span class="badge bg-success"><%= current_user.email %></span>
    </button>
    <h4> Users </h4>
    <%= turbo_stream_from "users" %>
    <div id="users">
      <%= render @users %>
    </div>
    <h4 class="pt-3 mb-3"> Rooms </h4>

    <%= turbo_stream_from "rooms" %>
    <%= render 'layouts/new_room_form' %>
    <div id="rooms" class="mt-3">
      <%= render partial: 'room', collection: @rooms %>
    </div>
    <%= link_to 'Back', 'javascript:history.back()', class: "btn btn-secondary bt-sm mt-3" %>
  </div>
  <div id="room_container" class="col-md-8 col-sm-8 bg-custom text-light">
    <% if @single_room %>
      <div id="single_room">

        <button type="button" class="btn btn-info bt-sm mb-3"><strong>Room</strong> <span class="badge bg-dark"> <%= @user&.email || @single_room.name %></span></button><%= turbo_stream_from @single_room %>
        <div id="messages">
          <%= render @messages %>
        </div>
        <%= render 'layouts/new_message_form' %>
      </div>
    <% end %>
  </div>




